/**
 * Small formatting helpers used across one-off scripts.
 * Re-exports from the canonical app lib to avoid duplication.
 */
export { humanReadableSize as humanBytes, formatSeconds as humanDuration } from "@/lib/format";
