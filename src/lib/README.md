# lib/

Shared utilities, configuration, and service clients for the Mission Control app.

Organize by domain:
- `db.ts` — database client
- `config.ts` — runtime configuration
- `pi/` — pi.dev SDK integration (future)
