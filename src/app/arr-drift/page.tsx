import { AppShell } from "@/components/layout/app-shell";
import { ArrDriftPage } from "@/components/arr-drift/arr-drift-page";

export default function Page() {
  return (
    <AppShell noScroll>
      <ArrDriftPage />
    </AppShell>
  );
}
